<nav class="navbar bg-dark navbar-light navbar-expand-lg" data-bs-theme="dark">
  <div class="container-fluid">
    <%= link_to 'Movie Review', root_path, class: 'navbar-brand' %>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavDropdown" aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNavDropdown">
      <ul class="navbar-nav">
        <li class="nav-item">
          <%= link_to 'Movies', root_path, class: "nav-link #{ current_page?(root_path) ? 'active' : '' }", 'aria-current': 'page' %>
        </li>
        <li class="nav-item">
          <%= link_to 'Profile', edit_user_registration_path, class: "nav-link #{ controller_name == 'registrations' ? 'active' : ''}", 'aria-current': 'page' %>
        </li>
      </ul>
      <ul class="navbar-nav ms-auto">
        <% if user_signed_in? %>
          <li class="nav-item">
            <%= button_to 'Logout', destroy_user_session_path, method: :delete, form: { data: { turbo_confirm: 'Are you sure?' } }, class: "btn btn-danger" %>
          </li>
        <% else %>
          <li class="nav-item">
            <%= link_to 'Sign in', user_session_path, class: "nav-link", 'aria-current': 'page' %>
          </li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>